 #include "holberton.h"
/**
 * _abs - print the value absolute.
 * @n: The integer .
 *
 * Return: the value absolute of integer.
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (-n);
}
}
